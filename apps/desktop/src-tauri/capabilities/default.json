{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum core permissions for the Rivloom main window.",
  "windows": ["main"],
  "permissions": ["core:window:default", "core:event:default"]
}
