# Temporary desktop icon

`icon.ico` only exists because `tauri-build` requires a Windows resource icon.
The geometric Rivloom mark is a temporary build asset, not an approved product
logo. Replace the SVG and generated icon before a branded public release.
